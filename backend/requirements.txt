fastapi>=0.115
uvicorn[standard]>=0.32
pydantic>=2.9
pydantic-settings>=2.6
sqlalchemy[asyncio]>=2.0
asyncpg>=0.30
alembic>=1.14

# Document text extraction (app/infrastructure/documents/text_extractor.py)
pymupdf>=1.24
python-docx>=1.1
openpyxl>=3.1
chardet>=5.2

# Supabase-issued JWT verification only (app/infrastructure/security/supabase_jwt.py).
# Verification-only: no password hashing, no application-issued tokens.
pyjwt[crypto]>=2.9

# multipart/form-data parsing, required by FastAPI's UploadFile/Form
# (app/api/v1/documents.py).
python-multipart>=0.0.12

# Narrow HTTP calls to Supabase Storage's REST API
# (app/infrastructure/storage/supabase_document_storage.py) -- not the
# supabase-py SDK, which would pull in a much larger dependency tree for
# functionality this sprint needs only two operations from. Previously a
# dev-only dependency (the ASGI test client); now also used at runtime.
httpx>=0.27
